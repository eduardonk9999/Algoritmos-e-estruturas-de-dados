let [x, y] = ['a', 'b'];

// Executar o código anterior é o mesmo que fazer o seguinte:
let x = 'a';
let y = 'b';

// Property shorthand, que é outra maneira de executar a desestruturação de objetos
let [x, y] = ['a', 'b'];
let obj = {x, y};

